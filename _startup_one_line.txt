echo "[ptero] begin startup"; w="$(pwd)"; r="${GIT_REPOS_ROOT:-${REPOS_ROOT:-resources}}"; case "$r" in /*) b="$r";; *) b="$w/$r";; esac; echo "[ptero] git repos base b=$b"; if [ -f "$w/ptero-git-sync.sh" ]; then echo "[ptero] running ptero-git-sync.sh"; bash "$w/ptero-git-sync.sh"; else echo "[ptero] missing ptero-git-sync.sh - set Multi-repo and Reinstall, or add the script manually"; fi; MULTI_ANY=0; [ -n "${GIT_YMAP_REPOURL}" ] && MULTI_ANY=1; [ -n "${GIT_VEHICLE_REPOURL}" ] && MULTI_ANY=1; [ -n "${GIT_SCRIPTS_REPOURL}" ] && MULTI_ANY=1; [ -n "${GIT_EUP_REPOURL}" ] && MULTI_ANY=1; if [ "$GIT_ENABLED" = "1" ] && [ "$MULTI_ANY" != "1" ] && [ -n "${GIT_REPOURL}" ] && [ -d "$b/.git" ]; then echo "[ptero] legacy single-repo pull in $b"; (cd "$b" && git pull) || true; fi; echo "[ptero] starting FXServer"; "$w/alpine/opt/cfx-server/ld-musl-x86_64.so.1" --library-path "$w/alpine/usr/lib/v8/:$w/alpine/lib/:$w/alpine/usr/lib/" -- "$w/alpine/opt/cfx-server/FXServer" +set citizen_dir "$w/alpine/opt/cfx-server/citizen/" +set sv_licenseKey "{{FIVEM_LICENSE}}" +set steam_webApiKey "{{STEAM_WEBAPIKEY}}" +set sv_maxClients {{MAX_PLAYERS}} +set sv_hostname "{{SERVER_HOSTNAME}}" +sets sv_projectName "{{SERVER_PNAME}}" +sets sv_projectDesc "{{SERVER_PDESC}}" +set onesync {{ONESYNC_ENABLED}} +setr voice_use3dAudio {{VOICE_USE3DAUDIO}} +setr voice_useSendingRangeOnly {{VOICE_USESENDINGRANGEONLY}} +setr voice_useNativeAudio {{VOICE_USENATIVEAUDIO}} +set sv_enforceGameBuild {{SV_GAMEBUILD}} $( [ "$TXADMIN_ENABLE" = "1" ] || printf %s '+exec server.cfg' )